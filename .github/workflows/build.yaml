name: build rpm
on: [push]
jobs:
  BuildRPM:
    runs-on: rocky-latest
    steps:
      - name: build
        run: | 
          gem install package_cloud
          echo make rpm
          package_cloud push tizitomm/cardano cardano-node.rpm